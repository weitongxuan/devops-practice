resources:
 containers:
   - container: server     
     image: devopstest/server:$(Build.SourceVersion)

steps:
- task: Docker@2
  inputs:
    containerRegistry: 'devopstest'
    container: server
    command: 'start'

- task: Bash@3
  inputs:
    targetType: 'inline'
    script: |
      pwd
      ls